version: '3'
services:
  app-dev:
    image: node-img:feature
    container_name: node-app-dev
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - PORT=3001
    restart: unless-stopped
